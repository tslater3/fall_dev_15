Instantiation = an instance can be created of a class. You can have multiple "instances" of a class.

Initialize = when .new is called on a class the Initialize method is called always. 

attr_accessor :name, :age (makes it so it can be written or read)